# Development server port
PORT=3000

# API Key for backend service authentication
# Generate this from the console app at /dashboard/api-keys
NEXT_PUBLIC_API_KEY=your-api-key

AGENT_ID=preferred-agent-id

# Server-side API URL (used by the authentication proxy)
SWAP_API_URL=http://api.dev.minmo.to/api/v1
